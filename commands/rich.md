---
description: Copy the last assistant response as rich text (RTF) to the macOS clipboard. Auto-extracts from the current session — no /copy needed.
allowed-tools: Bash(bash ~/.claude/scripts/rich-copy.sh*)
---

Run `bash ~/.claude/scripts/rich-copy.sh` and show the script's output verbatim (one line). Do not summarize, reformat, or add commentary.

The script auto-extracts the previous assistant response from the current Claude Code session — nothing else is required.
